namespace Hj.SutFactory.UnitTest;

public class InputBuilderAdvancedTests
{
  /*
  [Fact]
  public void Instance_GivenT_ReturnsInstance()
  {
    // arrange
    var inputBuilder = new SutBuilder().InputBuilder;

    ClassInput? classInput1 = null;
    ClassInput? classInput2 = null;

    // act
    inputBuilder.Advanced.Instance<ClassInput>().Configure(i => classInput1 = i);
    inputBuilder.Advanced.Instance<ClassInput>().Configure(i => classInput2 = i);

    // assert
    Assert.NotNull(classInput1);
    Assert.NotNull(classInput2);
    Assert.Same(classInput1, classInput2);
  }

  [Fact]
  public void SubstitutePartsOf_GivenT_ReturnsInstance()
  {
    // arrange
    var inputBuilder = new SutBuilder().InputBuilder;

    ClassInput? classInput1 = null;
    ClassInput? classInput2 = null;

    // act
    inputBuilder.Advanced.SubstitutePartsOf<ClassInput>().Configure(i => classInput1 = i);
    inputBuilder.Advanced.SubstitutePartsOf<ClassInput>().Configure(i => classInput2 = i);

    // assert
    Assert.NotNull(classInput1);
    Assert.NotNull(classInput2);
    Assert.Same(classInput1, classInput2);
  }

  [Fact]
  public void Substitute_GivenT_ReturnsInstance()
  {
    // arrange
    var inputBuilder = new SutBuilder().InputBuilder;

    IInterfaceInput? interfaceInput1 = null;
    IInterfaceInput? interfaceInput2 = null;

    // act
    inputBuilder.Advanced.Substitute<IInterfaceInput>().Configure(i => interfaceInput1 = i);
    inputBuilder.Advanced.Substitute<IInterfaceInput>().Configure(i => interfaceInput2 = i);

    // assert
    Assert.NotNull(interfaceInput1);
    Assert.NotNull(interfaceInput2);
    Assert.Same(interfaceInput1, interfaceInput2);
  }
  */
}
